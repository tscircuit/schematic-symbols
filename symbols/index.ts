export * from "./boxresistor"
import { boxresistor } from "./boxresistor"
import { getSvg, resize } from "../drawing"

export const symbols = {
  boxresistor,
  // Add other symbols here as they are created
}

export { getSvg, resize }
