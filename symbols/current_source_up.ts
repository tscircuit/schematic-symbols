import { modifySymbol } from "../drawing/modify-symbol/modify-symbol"
import current_source_right from "./current_source_right"

export default modifySymbol(current_source_right)
  .rotateRightFacingSymbol("up")
  .build()
